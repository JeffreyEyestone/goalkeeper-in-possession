# Continuation after calibration review

The user instructed “continue” after being informed that strict TF fails frozen modern-cohort calibration and that valuation was paused. Continue the remaining research while preserving this failure. Population-specific calibration is already permitted as a secondary sensitivity in the governing specification. It remains SECONDARY; the frozen WC2018 primary is not silently replaced. No no-refitting calibration claim is restored. All downstream frozen-TF values must carry the failed-calibration limitation.

Local calibration uses disjoint fixed match halves within each external cohort; each evaluated action receives a mapping trained on the opposite half. This is cross-fitted local recalibration, not unseen-population transfer. Its evaluation is exploratory because these populations were already inspected. The base classifier remains frozen.
